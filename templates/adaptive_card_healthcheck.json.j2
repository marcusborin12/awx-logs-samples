{
  "type": "message",
  "attachments": [
    {
      "contentType": "application/vnd.microsoft.card.adaptive",
      "contentUrl": null,
      "content": {
        "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
        "type": "AdaptiveCard",
        "version": "1.5",
        "body": [
          {
            "type": "TextBlock",
            "text": "🔍 Healthcheck Report - {{ awx_workflow_job_name | default('AWX Job') }}",
            "weight": "Bolder",
            "size": "Large"
          },
          {
            "type": "FactSet",
            "facts": [
              {% for item in HEALTHCHECK %}
              {
                "title": "{{ item.title }}",
                "value": "{% if item.value == 'OK' %}✅{% else %}❌{% endif %}"
              }{% if not loop.last %},{% endif %}
              {% endfor %}
            ]
          }
        ],
        "actions": [
          {
            "type": "Action.OpenUrl",
            "title": "Ver Logs no AWX",
            "url": "https://ansible.local/#/workflows/{{ awx_workflow_job_id | default('1') }}"
          }
        ]
      }
    }
  ]
}
